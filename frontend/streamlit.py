import streamlit as st
import requests

# response = requests.get("http://backend:8000/")  # Docker uses service name as hostname

st.title("Streamlit + FastAPI")
# st.write("Backend says:", response.json())
